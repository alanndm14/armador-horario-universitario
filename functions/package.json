{
  "name": "armador-horario-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check src/index.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
